// Check index files? There's this one, and also index.html
import React from 'react';
import 'semantic-ui-css/semantic.min.css'

// import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);